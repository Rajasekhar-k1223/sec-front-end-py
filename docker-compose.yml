services:
  frontend:
    build: .
    container_name: sentinelx-frontend
    ports:
      - "8082:80"
    environment:
      - VITE_API_URL=https://sentinelx-api.monitorix.co.in/api/v1
      - PORT=80
    networks:
      - watch-sec-network

networks:
  watch-sec-network:
    external: true
